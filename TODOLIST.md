# AeroQueue Development Tracker

## Этап 1: Фундамент и Базовое API

  ### Шаг 1: Окружение и Инфраструктура
- [x] **1.1. Инициализация репозитория и игнорирование файлов**
  - [x] Создать корневую папку проекта `aeroqueue`
  - [x] Выполнить команду `git init` в терминале
  - [x] Создать файл `.gitignore` и внести в него исключения для `.venv`, кэша Python (`__pycache__`) и секретов (`.env`)

- [x] **1.2. Виртуальное окружение (venv)**
  - [x] Создать изолированное окружение командой `python3 -m venv .venv`
  - [x] Активировать виртуальное окружение в терминале (`source .venv/bin/activate` или соответствующей командой для Windows)
  - [x] Обновить менеджер пакетов внутри окружения: `pip install --upgrade pip`

- [x] **1.3. Управление зависимостями**
  - [x] Создать файл `requirements.txt` в корне проекта
  - [x] Прописать базовый стек пакетов (`fastapi`, `uvicorn`, `pydantic-settings`, `sqlalchemy`, `asyncpg`, `alembic`, `ruff`) с фиксацией версий
  - [x] Установить пакеты командой `pip install -r requirements.txt`

- [x] **1.4. Стандарты кода и линтинг**
  - [x] Создать конфигурационный файл `ruff.toml` в корне проекта
  - [x] Настроить базовые правила валидации (длина строки 88 символов, правила `E`, `F`, `I`, `W`)
  - [x] Проверить работу линтера, запустив тестовую команду `ruff check .`

- [x] **1.5. Контейнеризация приложения (Dockerfile)**
  - [x] Создать файл `Dockerfile` на базе легковесного образа `python:3.12-slim`
  - [x] Настроить переменные окружения Python внутри контейнера (`PYTHONDONTWRITEBYTECODE`, `PYTHONUNBUFFERED`)
  - [x] Прописать логику копирования зависимостей, их установки без кэша и последующего копирования исходного кода
  - [x] Задать дефолтную команду запуска `uvicorn` с флагом `--reload`

- [x] **1.6. Оркестрация сервисов (Docker Compose)**
  - [x] Создать файл секретов `.env` с конфигурацией для PostgreSQL и строкой подключения `DATABASE_URL`
  - [x] Создать файл `docker-compose.yml`
  - [x] Описать сервис базы данных `db` на базе образа `postgres:16-alpine`, пробросить порт `5432` и настроить именованный volume для сохранения данных
  - [x] Описать сервис веб-приложения `api` со сборкой из локального Dockerfile, пробросом портов `8000:8000`, монтированием директории с кодом и зависимостью от сервиса `db`

  ### Шаг 2: Конфигурация и База данных (ORM)
- [x] **2.1. Создание структуры директорий приложения**
  - [x] Создать папку `app` в корне проекта
  - [x] Внутри `app` создать подпапки: `api/v1`, `core`, `models`, `schemas`
- [x] **2.2. Управление конфигурацией через Pydantic-Settings**
  - [x] Создать файл `app/core/config.py`
  - [x] Описать класс `Settings`, считывающий данные из `.env`
- [x] **2.3. Инициализация подключения к PostgreSQL**
  - [x] Создать файл `app/core/db.py`
  - [x] Настроить асинхронный движок `create_async_engine` и фабрику сессий `async_sessionmaker`
- [x] **2.4. Разработка SQL-модели задачи**
  - [x] Создать файл `app/models/task.py`
  - [x] Описать модель `Task` со всеми необходимыми полями (id, name, status, payload, result, timestamps)

  ### Шаг 3: Миграции Alembic
- [x] **3.1. Инициализация Alembic в проекте**
  - [x] Убедиться, что виртуальное окружение активировано
  - [x] Выполнить команду `alembic init -t async alembic` для создания структуры миграций
  - [x] Проверить появление папки `alembic` и файла `alembic.ini` в корне

- [x] **3.2. Настройка конфигурации миграций**
  - [x] Открыть `alembic/env.py` и добавить импорты `settings`, `Base` и модели `Task`
  - [x] Изменить строку `target_metadata = None` на `target_metadata = Base.metadata`
  - [x] Модифицировать функцию `run_migrations_online()`, чтобы URL базы данных динамически подтягивался из `settings.DATABASE_URL`

- [x] **3.3. Генерация первой миграции**
  - [x] Запустить контейнер с базой данных командой `docker compose up -d db`
  - [x] Выполнить команду генерации слепка: `alembic revision --autogenerate -m "init_tasks_table"`
  - [x] Проверить файл миграции в `alembic/versions/` на корректность описания полей таблицы `tasks`

- [x] **3.4. Применение миграции к БД**
  - [x] Выполнить команду `alembic upgrade head`
  - [x] Убедиться по логам в консоли, что миграция применилась без ошибок

### Шаг 4: Эндпоинты и Схемы (FastAPI)
- [x] **4.1. Создание точки входа приложения**
  - [x] Создать файл `app/main.py`
  - [x] Инициализировать экземпляр `FastAPI()` с кастомным заголовком "AeroQueue API"
  - [x] Написать базовый эндпоинт `GET /` (healthcheck), возвращающий статус работы сервиса

- [x] **4.2. Разработка Pydantic-схем (Валидация)**
  - [x] Создать файл `app/schemas/task.py`
  - [x] Описать схему `TaskCreate` для валидации входящих данных (name, payload)
  - [x] Описать схему `TaskResponse` для формирования ответа клиенту (со всеми полями из БД, включая UUID и статусы)
  - [x] Включить режим `from_attributes = True` в настройках схем для автоматической конвертации объектов SQLAlchemy

- [x] **4.3. Реализация роутера и логики эндпоинтов**
  - [x] Создать файл `app/api/v1/tasks.py`
  - [x] Инициализировать асинхронный роутер `APIRouter()`
  - [x] Подключить созданный роутер в `app/main.py` с префиксом `/api/v1/tasks`

- [x] **4.4. Разработка эндпоинта создания задачи (POST)**
  - [x] Написать асинхронную функцию для `POST /` эндпоинта
  - [x] Внедрить зависимость `get_db_session` через `Depends()`
  - [x] Реализовать логику: создание объекта `Task`, добавление в сессию, коммит (`commit`) и обновление данных (`refresh`)
  - [x] Проверить, что эндпоинт возвращает статус `201 Created` и схему `TaskResponse`

- [x] **4.5. Разработка эндпоинта получения задачи (GET)**
  - [x] Написать асинхронную функцию для `GET /{task_id}` эндпоинта
  - [x] Реализовать запрос `select()` к базе данных по переданному UUID
  - [x] Добавить обработку исключения: если задача не найдена, вызывать `HTTPException` со статусом `404 Not Found`

### Шаг 5: Простая Авторизация (JWT Stub)
- [x] **5.1. Эндпоинт аутентификации**
  - [x] Создать файл `app/api/v1/auth.py`
  - [x] Написать эндпоинт `POST /login`, принимающий фейковые username/password
  - [x] Возвращать фиксированный ("заглушку") JWT-токен в ответе

- [x] **5.2. Защита эндпоинтов**
  - [x] Создать зависимость `get_current_user`, которая проверяет наличие токена в заголовке `Authorization`
  - [x] Интегрировать эту зависимость в `POST /api/v1/tasks/`, делая авторизацию обязательной для создания задач